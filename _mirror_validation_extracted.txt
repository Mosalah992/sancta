Critical Pattern Issue: Broken Reply TemplatesStructural Corruption in Mirror Extraction & Template CompositionAnalysis Date: March 10, 2026Issue Type: STRUCTURAL - not covered by original 6 remediationsSeverity: HIGHExecutive Summary - What's Actually WrongThe sample reply reveals a STRUCTURAL template corruption issue:SAMPLE (from user):"About On paper, I'm 'local: what made you think that instead of the reverse?"PROBLEMS IDENTIFIED:1. "About" prefix: Not a natural sentence starter. Appears to be template text leaking through.2. Multiple clause fragments: "About" + "On paper" + "I'm 'local" concatenated without conjunction3. Broken quote: Single quote before "local: suggests malformed extraction4. Grammatical corruption: "About On paper, I'm 'local:" is not valid EnglishROOT CAUSE ANALYSIS:This is NOT a simple sanitization problem (removing code blocks).This is a TEMPLATE COMPOSITION problem:Likely Pattern:    Template: "About {mirror}: {question}?"    Mirror extraction: returned "On paper, I'm 'local" (incomplete, broken quote)    Result: Template format leaking + broken mirror = corrupted outputThis happens at a DIFFERENT level than the 6 remediations address.IMPLICATIONS:1. Original remediations (filter, guard, dedup) do NOT fix this2. This requires changes to TEMPLATE STRUCTURE and MIRROR EXTRACTION LOGIC3. This is more serious than we thought - it's systematic template corruption4. Every reply using this template will be brokenASSESSMENT:- Original 6 remediations: Fix output quality (generic, duplicate, contaminated)- This issue: Fixes template STRUCTURE (broken composition)- Both are needed for production-ready repliesDetailed Problem AnalysisThe Symptom: "About" Prefix RepetitionUser reports: "replies repeat with the following word: About"This means:1. NOT a one-off bug2. SYSTEMATIC: affects multiple replies3. PATTERN-BASED: specifically "About" word appears in template4. REPRODUCIBLE: happens consistently with certain reply typeThe Sample BreakdownReceived Sample:"About On paper, I'm 'local: what made you think that instead of the reverse?"Parsing this:About: Template text (not user-facing) -> Should not appear in outputOn paper,: Extracted mirror/context -> Fragment from original postI'm 'local:: Broken extraction with malformed quote -> Single quote suggests mid-word cut or API errorwhat made you think...: Question part (valid) -> This part is correctRoot Cause: Template Structure IssueMost likely pattern in code (sancta_generative.py):# BROKEN TEMPLATE (current):_MIRROR_RESPONSES = [    "About {mirror}: {question}?",    "About {mirror}, {reflection}",    "What about {mirror}? {question}"]# Example execution:mirror = extract_mirrors(post_content)  # Returns "On paper, I'm 'local"question = build_question(...)          # Returns "what made you think that instead of the reverse?"# Template substitution:template = "About {mirror}: {question}?"reply = template.format(mirror=mirror, question=question)# RESULT:"About On paper, I'm 'local: what made you think that instead of the reverse?"           ^^^^^^^^^^^^^^^^           BAD MIRROR (incomplete, malformed)[KEY ISSUE] The template structure "About {mirror}:" assumes mirror is a clean phrase. But mirror extraction is returning BROKEN/INCOMPLETE text like "On paper, I'm 'local" (notice the unclosed quote).Why Original 6 Remediations Don't Fix ThisAnalysis table:RemediationWhat it doesDoes it fix "About" issue?Why notSystem Text FilterRemoves Knowledge Capsule markersNODoes not address template structureShort-Reply GuardPrevents generic short repliesNOThis reply is NOT short; it's malformedMirror SanitizationRemoves code/JSON from mirrorsPARTIALRemoves CODE but doesn't fix INCOMPLETE extraction like "'local"Author DedupPrevents duplicate repliesNODedup doesn't affect template compositionCuriosity ExpansionExpands question poolNODoesn't affect template structureBot DetectionHandles non-human authorsNOUnrelated to template issue[CRITICAL] Mirror Sanitization is CLOSEST to fixing this, but only removes code blocks. It doesn't fix the core problem: incomplete/malformed mirror extraction that includes broken quotes and syntax errors.Additional Template Issues This RevealsIssue 1: About/What About Template PatternThe "About" prefix suggests templates like:_MIRROR_RESPONSES (BROKEN):- "About {mirror}: {question}?"- "What about {mirror}? {reflection}"- "About the {mirror} you mentioned..."- "Regarding {mirror}, {thought}"These templates work ONLY if {mirror} is a clean, complete phrase.If {mirror} is incomplete (e.g., "On paper, I'm 'local"), output breaks.Issue 2: Malformed Quotes in Mirror ExtractionThe single quote before "local suggests:1. User quoted something but extraction cut it off mid-quote2. API returned incomplete/corrupted text3. Regex extraction didn't handle quote boundaries correctlyExample sources:Original post might be:"On paper, I'm 'local' in my reasoning"Extraction might be:- Full: "On paper, I'm 'local' in my reasoning"  (GOOD)- Partial: "On paper, I'm 'local" (BAD - missing closing quote and rest)If extraction uses regex like: r"["'].*?["']"And post has: I'm 'local' in my reasoningThe regex might capture: I'm 'local (unclosed)Instead of: I'm 'local' (correct)Issue 3: Concatenation Without Grammar CheckingThe template just concatenates fragments:# Current approach (BROKEN):template = "About {mirror}: {question}?"result = template.format(mirror=mirror, question=question)# If mirror is incomplete/malformed, result is brokenmirror = "On paper, I'm 'local"  # INCOMPLETEquestion = "what made you think that..."result = "About On paper, I'm 'local: what made you think..."                ^^^^^^^^^^^^^^^^^^^                GRAMMATICALLY BROKEN# No validation that:- Mirror is a complete phrase- Mirror doesn't have broken quotes- Mirror + template creates grammatical sentenceSolution: Mirror Quality Validation (NEW REMEDIATION #7)Problem SummaryOriginal 6 remediations do NOT address this.Need NEW remediation: Mirror Quality ValidationSolution: Validate Mirror Before Template Use# NEW FUNCTION: Validate mirror qualitydef is_valid_mirror(mirror_text):    """Check if mirror is suitable for template composition."""        if not mirror_text or len(mirror_text) < 3:        return False        # Check for broken quotes (unclosed quote marks)    single_quotes = mirror_text.count("'") + mirror_text.count("`")    double_quotes = mirror_text.count('"')        # Both should be even (opening + closing pairs)    if single_quotes % 2 != 0 or double_quotes % 2 != 0:        return False  # Broken quotes        # Check for incomplete code/syntax    if any(bracket in mirror_text for bracket in ['{', '[', '(']):        # If opening bracket, closing bracket must be present        opens = {'[': ']', '{': '}', '(': ')'}        for open_br, close_br in opens.items():            if open_br in mirror_text and close_br not in mirror_text:                return False  # Unclosed bracket        # Check for malformed fragments    # If mirror is very short and contains only punctuation/quotes, reject    words = [w for w in mirror_text.split() if w.isalpha()]    if len(words) == 0:        return False  # No actual words        # Check for template placeholders (shouldn't be in mirror)    if any(placeholder in mirror_text for placeholder in ['{', '}', '?', '*']):        if mirror_text.count('{') != mirror_text.count('}'):            return False  # Unbalanced template syntax        return True# Usage: In _extract_mirrorsdef _extract_mirrors_validated(content, topic_keywords, state):    """Extract mirrors and validate before returning."""    candidates = extract_raw_mirror_candidates(content, topic_keywords)    valid_mirrors = []        for cand in candidates:        # First sanitize (remove code blocks, JSON)        clean = _sanitize_mirror(cand)                # Then validate (check for broken quotes, syntax)        if clean and is_valid_mirror(clean):            valid_mirrors.append(clean)        if not valid_mirrors:        return None  # No valid mirrors; don't use template with broken mirror        return valid_mirrors# Usage: In template selectiondef build_mirrored_reply(post, topic_match, state):    """Build reply only if mirror is valid."""    mirrors = _extract_mirrors_validated(post['content'], topic_match['topic'], state)        if not mirrors:        # No valid mirrors; use question-led or other strategy instead        return build_question_reply(post, state)        mirror = random.choice(mirrors)        # Now safe to use in template    reply = f"About {mirror}: {build_question(post, state)}?"        return replyImplementation as Bonus Fix (Post-Remediation #6)This is CRITICAL but quick to implement:PlanScopeTimelineCurrent Plan6 remediations2-3 weeksRevised Plan6 remediations + Mirror Validation2.5 weeks (add 2-3 hours)Add toPhase 1 (Day 2)After mirror sanitizationImplementation Steps (Solo Dev)1. Add is_valid_mirror() function to sancta_generative.py (30 min)   - Check for broken quotes (unmatched " or ')   - Check for unclosed brackets ({, [, ()   - Check that mirror has actual words (not just punctuation)   - Return False if any validation fails2. Modify _extract_mirrors to call is_valid_mirror() (15 min)   - After sanitization, validate before appending to list   - If all mirrors fail validation, return None   - Caller will switch to non-mirror reply strategy3. In build_mirrored_reply, check if mirrors exist (5 min)   - If None returned, don't use mirror template   - Fallback to question-led or direct reply4. Test with 10 samples (15 min)   - Create test cases with broken quotes: "I'm 'local" -> REJECT   - Create test cases with complete quotes: "I'm local" -> ACCEPT   - Create test cases with unclosed brackets: "{something" -> REJECTTotal: 65 minutes (add to Day 2 of implementation)Updated Solo Dev Timeline (Revised)Adding Mirror Validation to timeline:WhenWhatTimePriorityWeek 1 Day 1System Text Filter + Dedup Persistence10 hoursMUST DOWeek 1 Day 2 AMMirror Sanitization + VALIDATION (NEW)3.5 hoursCRITICALWeek 1 Day 2 PMCuriosity Expansion1 hourNice to haveWeek 1 Day 3 AMShort-Reply Guard3 hoursHigh impactWeek 1 Day 3 PMBot Detection + Author Dedup4 hoursMedium impactWeek 2Testing + deployment14 hoursValidation[CHANGE] Mirror Sanitization moved from 3 hours to 3.5 hours. Validation adds 30 min but CRITICAL for fixing "About" issue.Conclusion & Action ItemsWHAT YOU DISCOVERED:The "About" prefix repetition reveals a STRUCTURAL problem:- Templates are using broken mirrors (incomplete, malformed quotes)- Templates concatenate fragments without validation- Results are grammatically broken sentencesWHY THIS MATTERS:1. It's SYSTEMATIC (affects multiple replies, not one-off)2. It's REPRODUCIBLE (happens consistently)3. It's NOT FIXED by the original 6 remediations4. It requires changes to MIRROR VALIDATION LOGICTHE FIX:Add Mirror Quality Validation (NEW REMEDIATION #7):- Validate mirrors before using in templates- Check for broken quotes, unclosed brackets- If mirror fails validation, don't use template; fallback to safe strategyIMPLEMENTATION:- Add is_valid_mirror() function (30 min)- Modify _extract_mirrors to validate (15 min)- Test with broken/valid mirrors (15 min)- Total: 1 hour, add to Day 2 of timelineACTION ITEMS:1. Add Mirror Quality Validation to implementation plan2. Implement after Mirror Sanitization (same day, afternoon)3. Test specifically for "About..." pattern - should disappear4. Measure: count replies starting with "About" (should drop to 0)This is a HIGH-VALUE fix: probably eliminates 2-3 percent of malformed replies.