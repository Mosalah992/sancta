Sound pack for the "office_peon" notification theme.

Files (MP3 format — update manifest.json if you change these):
- office_peon/ready.mp3        # session.start, session.end, mood.change
- office_peon/work_done.mp3    # security.alert, redteam.alert, task.error
- office_peon/job_done.mp3     # task.complete (cycle finished)

Supported formats: .mp3, .wav, .ogg (pygame.mixer handles all three).
